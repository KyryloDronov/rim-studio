/** Shared pricing tab switch — heavy frost → reveal. */

export const PRICING_BLUR_REST_PX = 4;
export const PRICING_BLUR_PEAK_PX = 40;

export const PRICING_WASH_REST = 0.12;
export const PRICING_WASH_PEAK = 0.72;

/** Blur holds heavy, then falls in sync with the table cascade. */
export const PRICING_REVEAL_DELAY_S = 0.28;
export const PRICING_BLUR_SETTLE_S = 1.35;

export const PRICING_BG_IMAGE_ENTER_S = 1.45;
export const PRICING_BG_IMAGE_EXIT_S = 0.55;
export const PRICING_BG_IMAGE_SCALE_FROM = 1.04;
