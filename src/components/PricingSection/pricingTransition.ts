/** Shared pricing tab switch — heavy frost → reveal. */

export const PRICING_BLUR_REST_PX = 4;
/** Mild peak during tab crossfade — heavy blur + wash was washing out to white. */
export const PRICING_BLUR_PEAK_PX = 22;

export const PRICING_WASH_REST = 0.12;
export const PRICING_WASH_PEAK = 0.28;

/** Blur holds, then falls in sync with the table cascade. */
export const PRICING_REVEAL_DELAY_S = 0.28;
export const PRICING_BLUR_SETTLE_S = 1.35;

/** Overlapping crossfade — old dissolves while new fades in (no white gap). */
export const PRICING_BG_IMAGE_CROSSFADE_S = 0.9;
export const PRICING_BG_IMAGE_SCALE_FROM = 1.03;
