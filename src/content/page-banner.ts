/** Marks the top-of-page hero/banner region — header pin zone is derived from its bottom edge. */
export const PAGE_BANNER_ATTR = "data-page-banner";
export const PAGE_BANNER_SELECTOR = `[${PAGE_BANNER_ATTR}]`;
